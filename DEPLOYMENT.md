# 🚀 راهنمای استقرار روی سرور

این راهنما برای بردن نسخه‌ی جدید روی سرور واقعی است.

> **خلاصه در یک نگاه:** ساختار پروژه عوض نشده. فقط فایل اضافه شده و
> چند ستون به دیتابیس افزوده شده است. هیچ فایلی حذف یا جابه‌جا نشده و
> هیچ جدول یا ستونی از بین نرفته است. مسیرها و آدرس‌ها همان قبلی‌اند.

---

## ⚠️ پیش از هر چیز: دو کاری که فقط شما می‌توانید انجام دهید

رمز دیتابیس و کلید JWT قبلی **در تاریخچه‌ی git باقی مانده‌اند**. حذفشان از
کد کافی نیست؛ هر کسی که به تاریخچه دسترسی داشته باشد می‌تواند از آن‌ها
استفاده کند — با کلید JWT قدیمی می‌شود برای خود توکن مدیر ساختمان ساخت.

1. **رمز دیتابیس را در کنترل‌پنل هاست عوض کنید.**
2. **یک کلید JWT تازه بسازید:**
   ```bash
   openssl rand -base64 48
   ```

هر دو مقدار جدید در گام ۲ داخل `.env` قرار می‌گیرند.

> نکته: با عوض‌شدن `JWT_SECRET` همه‌ی کاربران یک بار از حساب خارج می‌شوند
> و باید دوباره وارد شوند. این طبیعی است.

---

## گام ۱ — گرفتن کد جدید

```bash
cd /path/to/your/site        # مثلاً /home/user/public_html/b
git pull origin main         # یا شاخه‌ای که مرج کرده‌اید
composer install --no-dev --optimize-autoloader
```

اگر روی هاست اشتراکی کامپوزر ندارید، پوشه‌ی `vendor/` را از روی سیستم خودتان
آپلود کنید. **حتماً** فایل‌های `vendor/composer/autoload_*.php` را هم بفرستید —
در این نسخه تغییر کرده‌اند (کلاس `AppConfig` به autoload اضافه شده است).

---

## گام ۲ — ساخت فایل `.env`

```bash
cp .env.example .env
nano .env
```

حداقل این چهار مقدار را پر کنید:

```ini
APP_ENV=production
APP_URL=https://file.falnic.com/b
JWT_SECRET=<کلیدی که در بخش بالا ساختید>
DB_PASSWORD=<رمز جدید دیتابیس>
```

اگر پیامک استفاده می‌کنید، این‌ها را هم پر کنید:

```ini
SMS_USERNAME=...
SMS_PASSWORD=...
SMS_BODY_ID=...
```

سپس دسترسی فایل را محدود کنید:

```bash
chmod 600 .env
```

> **مهم:** اگر `.env` را نسازید، برنامه عمداً بالا نمی‌آید و پیام صریح
> می‌دهد که کدام متغیر کم است. این رفتار عمدی است تا سایت با تنظیمات
> ناامن اجرا نشود.

---

## گام ۳ — اجرای مایگریشن‌ها

```bash
php scripts/migrator.php --status    # اول ببینید چه چیزی در انتظار است
php scripts/migrator.php             # سپس اجرا
```

سه مایگریشن جدید اعمال می‌شود:

| شماره | کار |
|---|---|
| `025_charge_modes` | ستون‌های حالت شارژ: `charge_mode`، `charge_per_person`، `residents_count`، `custom_charge` |
| `026_otp_auth` | جدول `otp_codes` + nullable شدن `users.name` و `users.password_hash` |
| `027_rate_limits` | جدول `rate_limits` برای جلوگیری از حمله به رمز عبور |

همه فقط **اضافه** می‌کنند؛ هیچ داده‌ای حذف نمی‌شود.

> اگر `migrator.php` قبلاً برایتان کار نمی‌کرد، دلیلش یک باگ بود که در این
> نسخه رفع شد: به پوشه‌ی اشتباهی نگاه می‌کرد و بی‌صدا «تمام شد» می‌گفت
> بدون اینکه چیزی اجرا کند.

**جایگزین:** اگر ترجیح می‌دهید مستقیم SQL بزنید:
```bash
mysql -u <user> -p <database> < all_migrations.sql
```

---

## گام ۴ — پوشه‌ی لاگ

```bash
mkdir -p storage/logs
chmod -R 775 storage
```

لاگ‌ها در `storage/logs/app-YYYY-MM-DD.log` نوشته می‌شوند (فرمت JSON، یک
رکورد در هر خط). اگر این پوشه قابل نوشتن نباشد، برنامه خطا نمی‌دهد و به
`error_log` پیش‌فرض PHP برمی‌گردد.

---

## گام ۵ — بررسی سلامت

```bash
php scripts/healthcheck.php
```

این دستور نسخه‌ی PHP، افزونه‌ها، تنظیمات حیاتی، دسترسی نوشتن، وجود
جدول‌ها و اعمال‌شدن مایگریشن‌ها را بررسی می‌کند.

خروجی موفق چیزی شبیه این است:

```
✔ سامانه سالم است
```

اگر مشکلی باشد، دقیقاً می‌گوید چه چیزی کم است و کد خروجی `1` برمی‌گرداند.

---

## گام ۶ — آزمایش دستی

پس از استقرار این‌ها را در مرورگر امتحان کنید:

- [ ] `https://your-site/b/auth.php` باز می‌شود
- [ ] ورود با شماره موبایل و کد یک‌بارمصرف کار می‌کند (پیامک می‌رسد)
- [ ] ورود با رمز عبور برای حساب‌های قدیمی کار می‌کند
- [ ] `login.php` به `auth.php` ریدایرکت می‌شود
- [ ] یک اطلاعیه جدید از طریق پنجره‌ی مودال ثبت و سپس ویرایش می‌شود
- [ ] در صفحه‌ی مالی، هر سه حالت شارژ (ثابت / نفری / دلخواه) درست محاسبه می‌شود
- [ ] کاربر مستأجر دکمه‌های مدیریتی را نمی‌بیند
- [ ] فایلی در `storage/logs/` ساخته شده است

---

## ❓ اگر مشکلی پیش آمد

**سایت سفید شد یا خطای ۵۰۰ داد**
```bash
tail -50 storage/logs/app-$(date +%F).log
```
معمولاً یعنی `.env` ناقص است. `php scripts/healthcheck.php` را بزنید.

**پیام «JWT_SECRET تنظیم نشده است»**
فایل `.env` ساخته نشده یا `JWT_SECRET` خالی است. گام ۲ را انجام دهید.

**همه‌ی فرم‌ها خطای ۴۱۹ می‌دهند**
این یعنی محافظت CSRF فعال شده ولی نشست کاربر قدیمی است. یک بار خروج و
ورود دوباره مشکل را حل می‌کند. اگر ادامه داشت، مطمئن شوید `session.save_path`
روی سرور قابل نوشتن است.

**پیام «کلاس AppConfig پیدا نشد»**
فایل‌های `vendor/composer/autoload_*.php` را آپلود نکرده‌اید، یا
`composer dump-autoload` را اجرا نکرده‌اید.

**ورود کاربر مسدود شد**
محدودیت نرخ فعال شده (۵ تلاش ناموفق در ۱۵ دقیقه). ۱۵ دقیقه صبر کنید یا:
```sql
DELETE FROM rate_limits;
```

---

## 🔙 بازگشت به نسخه‌ی قبل

اگر لازم شد برگردید:

```bash
git log --oneline -5          # کامیت قبلی را پیدا کنید
git checkout <commit-hash>
```

مایگریشن‌ها نیازی به بازگردانی ندارند چون فقط ستون و جدول **اضافه**
کرده‌اند و کد قدیمی با وجودشان مشکلی ندارد.

---

## 📋 آیا ساختار پروژه عوض شد؟

**نه.** خلاصه‌ی دقیق تغییرات:

### آنچه عوض **نشد**
- ساختار پوشه‌ها (`src/`، `includes/`، `public/`، `config/`، `database/`)
- مسیر و آدرس صفحات — همه سرجای خودشان
- مسیرهای API — همه‌ی endpointهای قبلی دست‌نخورده
- هیچ فایلی حذف یا تغییر نام نداد
- هیچ جدول یا ستونی از دیتابیس حذف نشد

### آنچه اضافه شد
| مسیر | چیست |
|---|---|
| `auth.php` | صفحه‌ی ورود یکپارچه (جایگزین login/register) |
| `.env.example` | نمونه‌ی تنظیمات محیطی |
| `DEPLOYMENT.md` | همین فایل |
| `src/Core/Logger.php` | لاگ‌گیر مرکزی |
| `src/Services/OtpService.php` | کد یک‌بارمصرف |
| `src/Services/RateLimiter.php` | محدودیت نرخ |
| `scripts/healthcheck.php` | بررسی سلامت |
| `includes/modal.php` + `includes/_*_form_fields.php` | زیرساخت پنجره‌های مودال |
| `database/migrations/025..027` | سه مایگریشن جدید |
| `tests/Integration/` | ۳۰۸ تست |
| `ci/` | workflow آماده‌ی CI |

### آنچه رفتارش عوض شد
- `login.php` و `register.php` حالا فقط **ریدایرکت ۳۰۱** به `auth.php` هستند
  (لینک‌های قدیمی همچنان کار می‌کنند)
- رمز دیتابیس و کلید JWT از `config/app.php` به `.env` منتقل شدند
- `APP_ENV` پیش‌فرضش `production` شد (قبلاً `development` بود)
- کلید ارسال فرم‌ها در همه‌جا `form_action` شد (قبلاً سه اسم مختلف بود)
- بررسی SSL پیش‌فرض روشن شد
